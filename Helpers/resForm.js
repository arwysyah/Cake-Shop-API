module.exports = {
  getAll: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getExperience: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },getBooking: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  bookingRoom: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getLimit: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  filterProduct: (res,response, name)=>{
    const form = {
        name,
        response,
    

    };
    res.json(form);
  },
  getByStatus: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getStatus: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getGenre: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getTitle: (res, response, title) => {
    const form = {
      title,
      response
    };
    res.json(form);
  },
  getByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getByGenre: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  postProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  deleteProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  putProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  updateStatus: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getHistory: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  postHistory: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },

  getWishlist: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  postWishlist: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  deleteWishlist: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  }
};
